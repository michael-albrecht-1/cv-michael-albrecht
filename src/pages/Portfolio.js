import React from 'react';
import Navigation from '../components/Navigation';

const Portfolio = () => {
    return (
        <div>
            <Navigation />
            portfolio page
        </div>
    );
};

export default Portfolio;